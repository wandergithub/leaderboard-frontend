import axios from "axios";

// Actions

// Reducer
const Reducer = (state = { players: [], groups: [] }, action = {}) => {
  switch (action.type) {
    case "":
      break;

    default:
      return state;
  }
};

// Action Creators


// Thunks

export default Reducer;
